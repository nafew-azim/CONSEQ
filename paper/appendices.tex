\appendix

\section{Per-Run Manifest}
\label{app:manifest}

Every run in the release, with the retention and consequence counts that determine whether it
supports a claim. Runs marked $\dagger$ retain fewer than 25 problems and are excluded from headline
claims; they are retained because coverage of older and non-code-specialised models is itself
informative about where this instrument can be applied at all.

\begin{table}[H]
\centering\footnotesize
\begin{tabular}{lllrrrr}
\toprule
Run & Model & Family & Lang & Problems & Positions & Consequential \\
\midrule
\texttt{o\_scgo} & starcoder2-3b & BigCode & go & 35 & 2,346 & 289 \\
\texttt{o\_scjava} & starcoder2-3b & BigCode & java & 47 & 3,006 & 276 \\
\texttt{o\_v2sc} & starcoder2-3b & BigCode & js & 57 & 4,555 & 646 \\
\texttt{o\_scrust} & starcoder2-3b & BigCode & rs & 38 & 2,269 & 230 \\
\texttt{o\_scts} & starcoder2-3b & BigCode & ts & 64 & 4,639 & 521 \\
\texttt{o\_v2ds} & deepseek-coder-1.3b-base & DeepSeek & js & 49 & 3,613 & 429 \\
\texttt{o\_v2dspy} & deepseek-coder-1.3b-base & DeepSeek & py & 48 & 2,776 & 285 \\
\texttt{outdt} & deepseek-coder-1.3b-base & DeepSeek & ts & 49 & 3,604 & 393 \\
\texttt{o\_smol} & SmolLM2-1.7B & HuggingFace & js & 38 & 3,014 & 342 \\
\texttt{o\_g33} & granite-3.3-2b-base & IBM & js & 47 & 3,565 & 530 \\
\texttt{o\_gr} & granite-3b-code-base & IBM & js & 55 & 4,390 & 636 \\
\texttt{o\_incoder} & incoder-1B & Meta & js & 11 & 237 & 24 $\dagger$ \\
\texttt{o\_phi2} & phi-2 & Microsoft & js & 20 & 1,444 & 151 $\dagger$ \\
\texttt{o\_v2nc} & Qwen2.5-1.5B & Qwen & js & 58 & 3,286 & 494 \\
\texttt{o\_v205} & Qwen2.5-Coder-0.5B & Qwen & js & 46 & 2,437 & 356 \\
\texttt{o\_mgo} & Qwen2.5-Coder-1.5B & Qwen & go & 56 & 2,504 & 255 \\
\texttt{o\_v2go} & Qwen2.5-Coder-1.5B & Qwen & go & 38 & 2,261 & 293 \\
\texttt{o\_mjava} & Qwen2.5-Coder-1.5B & Qwen & java & 80 & 4,068 & 404 \\
\texttt{o\_v2java} & Qwen2.5-Coder-1.5B & Qwen & java & 63 & 3,822 & 434 \\
\texttt{o\_v2mbpp} & Qwen2.5-Coder-1.5B & Qwen & js & 218 & 11,670 & 1,150 \\
\texttt{o\_v2js} & Qwen2.5-Coder-1.5B & Qwen & js & 77 & 5,097 & 784 \\
\texttt{o\_mphp} & Qwen2.5-Coder-1.5B & Qwen & php & 189 & 15,863 & 2,178 \\
\texttt{o\_php} & Qwen2.5-Coder-1.5B & Qwen & php & 73 & 5,584 & 991 \\
\texttt{o\_mpl} & Qwen2.5-Coder-1.5B & Qwen & pl & 130 & 6,650 & 2,141 \\
\texttt{o\_perl} & Qwen2.5-Coder-1.5B & Qwen & pl & 43 & 3,138 & 961 \\
\texttt{o\_v2py} & Qwen2.5-Coder-1.5B & Qwen & py & 66 & 2,937 & 412 \\
\texttt{o\_rb} & Qwen2.5-Coder-1.5B & Qwen & rb & 53 & 2,208 & 475 \\
\texttt{o\_mrb} & Qwen2.5-Coder-1.5B & Qwen & rb & 76 & 2,120 & 326 \\
\texttt{o\_rust} & Qwen2.5-Coder-1.5B & Qwen & rs & 59 & 3,290 & 385 \\
\texttt{o\_mts} & Qwen2.5-Coder-1.5B & Qwen & ts & 90 & 7,427 & 503 \\
\texttt{o\_v2ts} & Qwen2.5-Coder-1.5B & Qwen & ts & 76 & 5,703 & 537 \\
\texttt{o\_v23b} & Qwen2.5-Coder-3B & Qwen & js & 88 & 6,057 & 914 \\
\texttt{o\_qwen3} & Qwen3-1.7B & Qwen & js & 51 & 3,426 & 556 \\
\texttt{o\_codegen} & codegen-2B-mono & Salesforce & js & 15 & 796 & 108 $\dagger$ \\
\texttt{o\_stable} & stable-code-3b & Stability & js & 53 & 3,984 & 458 \\
\midrule
\textbf{Total} & & & & & \textbf{143,786} & \\
\bottomrule
\end{tabular}
\caption{Per-run manifest. The run identifier is the output directory in the release, so every
row of \texttt{conseq.jsonl} traces to the session that produced it; this is what distinguishes
the several model-language pairs measured more than once (a second benchmark, or a
counterfactual-rule variant). Runs marked $\dagger$ retain fewer than 25 problems, are flagged
\texttt{underpowered} in the manifest file, and are excluded from headline claims.}
\label{tab:manifest}
\end{table}

Retention is the binding constraint on coverage; compute is not. A model must produce a
verified-correct greedy trajectory before any position can be measured, so a model that cannot solve
a task contributes nothing. InCoder-1B retains 11 of 161 HumanEval-JS tasks; Qwen2.5-Coder-1.5B
retains 77. This asymmetry is a property of the measurement, not a sampling choice, and it means
\textsc{Conseq} is systematically better populated for capable models.


\section{Column Schema}
\label{app:schema}

Every row of \texttt{conseq.jsonl} is self-describing: provenance travels with the measurement, so
any subset is reconstructable without consulting the manifest.

\begin{table}[H]
\centering\small
\begin{tabular}{llp{8.2cm}}
\toprule
Column & Type & Meaning \\
\midrule
\texttt{problem} & str & benchmark task identifier \\
\texttt{pos} & int & token index within the generated completion \\
\texttt{n\_pos} & int & completion length in tokens \\
\texttt{tok} & str & the token the model emitted \\
\texttt{alt} & str & the counterfactual token (the model's second choice) \\
\texttt{verdict} & str & \texttt{pass} / \texttt{fail:<Class>} / \texttt{type\_error:<Class>} / \texttt{parse\_error} / \texttt{timeout} \\
\texttt{err} & str & error class parsed from the runtime's stderr \\
\texttt{K\_syn} & bool & the substitution fails to parse \\
\texttt{K\_type} & bool & parses, fails static type checking; always false where no separate phase exists \\
\texttt{K\_sem} & bool & parses, type-checks, fails its tests \\
\texttt{K\_timeout} & bool & execution exceeded its budget: \emph{unmeasured}, never consequential \\
\texttt{entropy} & float & Shannon entropy of the model's distribution at this position \\
\texttt{margin} & float & $p(\text{top-1}) - p(\text{top-2})$ \\
\texttt{suffix\_len} & int & tokens remaining after this position \\
\texttt{equivalent} & bool & substitution is AST-equivalent after normalisation \\
\texttt{lang} & str & language identifier \\
\texttt{typing} & str & \texttt{dynamic} / \texttt{gradual} / \texttt{static} \\
\texttt{model} & str & model identifier \\
\texttt{family} & str & model family \\
\texttt{params} & str & parameter count \\
\texttt{iv} & int & instrument version (2 throughout this release) \\
\bottomrule
\end{tabular}
\caption{Schema of \texttt{conseq.jsonl}. Two columns carry conditions, not values, and are the
most common source of misuse: \texttt{K\_type} is language-conditional, and \texttt{K\_timeout}
marks missing data and not a negative result.}
\label{tab:schema}
\end{table}

\paragraph{Two columns that are not what they look like.} \texttt{K\_type} is \texttt{false} both
when a type checker passed the program and when no type checker exists, so any cross-language
aggregate must condition on \texttt{lang}. \texttt{K\_timeout} marks a measurement that did not
complete; treating those rows as ``not consequential'' inflates the pass rate, and treating them as
consequential produced the contradiction recorded in B9.

\section{Corrections}
\label{app:corrections}

Eleven results in this project were believed before they were found to be artifacts of the
instrument. We record them because a dataset paper's central claim is that its numbers mean what
they say, and the credible form of that claim is a demonstration that the failure modes are known,
and not an assertion that none occurred.

Each entry gives the symptom that exposed the defect. A pattern runs through the first ten and is
worth stating as methodology: \textbf{every one was caught by a number being implausibly clean, and
not by an error}. Nothing crashed. Each produced well-formed output that would have survived casual
inspection. B11 is the exception, and it marks where that heuristic stops working.

\paragraph{B1. End-of-sequence token decoded into program text.} The literal string
\texttt{<|endoftext|>} reached the executed program, making every candidate a syntax error. This
silently discarded roughly 70\% of trajectories; retention rose from 18 to 77 problems on the fix.
\emph{Symptom:} an implausibly high parse-failure rate on programs the model had written
competently. Caught only by logging \emph{why} problems were skipped instead of counting how many.

\paragraph{B2. Continuation arms reconstructed from the wrong prefix.} The model was conditioned on
$y_{\leq t}$ but the program was reassembled from $y_{<t}$, dropping the token under test; the
counterfactual arm dropped the substituted token entirely and measured nothing.
\emph{Symptom:} the factual arm passed 9\% of the time when continuing from a known-correct prefix
must usually succeed. Now asserted at runtime.

\paragraph{B3. ``Consequence does not compose'' (retracted).} Reported as 0 of 44 programs
surviving simultaneous substitution, and briefly treated as the project's most important finding.
``Free'' had been defined as \emph{not} $K^{\mathrm{sem}}$, which silently included artifact
positions that genuinely break programs. \emph{Symptom:} a \emph{single} substitution at a
supposedly free position passed only 66.8\% of the time when it must pass approximately always.
Under the corrected definition, $m=1$ passes 100.0\% and the aggregate is 93.2\%, the opposite
conclusion.

\paragraph{B4. Decoding oracle handicapped against its own controls.} The oracle committed only at
$K^{\mathrm{sem}}$ positions, leaving artifact-breaking positions inside its sampled set, and
committed at 14.9\% of positions while controls committed at 42.1\%. \emph{Symptom:} a
ground-truth-driven policy performing worse than random, which is impossible if the comparison is
fair. Arms are now asserted to commit at identical counts.

\paragraph{B5. Bug-localisation result driven by the injection procedure.} Token surprisal appeared
to localise injected bugs almost perfectly. Bugs were injected as the model's second choice, making
the injected token the single improbable token in an otherwise top-1 sequence; surprisal was
detecting the injection, not the bug. Reported but never claimed as a result.

\paragraph{B6. Python requested from a dataset that does not contain it.} MultiPL-E translates
\emph{from} Python into other languages, so Python is its source and never a target. Python requires
the original HumanEval, which uses a different schema.

\paragraph{B7. Python programs executed by the JavaScript runtime.} The splice batch was routed
through a helper that had never been given the language parameter. The trajectory-validity check
\emph{did} pass it, so problems were correctly retained and only the splice arm was wrong, which is
why the run looked healthy. \emph{Symptom:} 0 consequential positions of 2{,}937, with 100\% parse
errors. A program valid enough to pass its own tests cannot have zero parseable single-token
variants; that invariant is now asserted and aborts the run.

\paragraph{B8. Go tasks executed as programs rather than tests.} MultiPL-E ships Go tasks as Go
\emph{test} files; running them with \texttt{go run} compiles a package with no \texttt{main} and
executes no assertions. \emph{Symptom:} 0 of 154 problems retained. The Go self-check had passed
because it used \texttt{os.Exit} instead of the \texttt{testing} package. \textbf{A self-check that
does not reproduce the production format manufactures confidence}.

\paragraph{B9. Timeouts counted as semantic consequence.} Java showed a 19.0\% timeout rate against
at most 1.4\% elsewhere, because \texttt{javac} plus \texttt{java} per splice with eight workers on
four cores is contention, not infinite loops. This produced a $K^{\mathrm{sem}}$ of 26.9\% that
contradicted every other typed language; excluding timeouts gives 11.2\%. \emph{Symptom:}
punctuation at 21.8\% and whitespace at 32.4\% consequential, against roughly 1\% and 5\% in every
other language. Worker counts and timeouts are now scaled per toolchain.

\paragraph{B10. Linker failure classified as a syntax error.} Rust splices were sorted by
diagnostic shape: a bare \texttt{error:} meant syntax, \texttt{error[E\ldots]} meant a type or borrow
fault. A \texttt{rustc} linker failure carries neither form, so environment failures such as a
missing system linker under contention were being recorded as $K^{\mathrm{syn}}$. The symptom was a Rust
syntax rate that moved with machine load instead of with the substitution. Linker failures are now
routed to \texttt{timeout}, the class reserved for unmeasured positions.

\paragraph{B11. Rho-1's excess loss implemented with reversed operands.} \citet{lin2024rho}
define the excess loss as $L_\Delta(x_i) = L_\theta(x_i) - L_{\mathrm{RM}}(x_i)$, the current
model's loss minus the reference model's, and Selective Language Modeling trains on the top $k\%$.
Our implementation computed $L_{\mathrm{RM}} - L_\theta$, which is the same quantity negated, so
scoring it in the ``select high'' direction selected precisely the tokens the method discards.
\emph{Symptom:} excess loss was the sole criterion of eleven with a positive correlation, and the
sole one that did not improve when reversed: an isolated exception in an otherwise uniform table,
which is a shape worth distrusting. Scored as published, $\rho$ moves from $+0.067$ to $-0.067$ and
CC@25 from 23.2\% to 12.9\%. The selftest now asserts the sign as well as the zero: excess loss must
vanish when reference and current model agree, \emph{and} be positive when the current model fits
the token worse. Unlike B1--B10 this defect was found by reading the source paper, not by an
implausible number, which is why the criterion definitions are now stated as formulas in
Appendix~\ref{app:criteria} instead of described in prose.

\paragraph{The diagnostic that generalises.} In B3, B7, and B9 the tell was identical: positions that
ought to be inert (punctuation, whitespace, formatting) appearing consequential. In a working
instrument these sit near 1\%. We recommend the check to anyone extending this work: \textbf{if
inert tokens look consequential, the instrument is broken, not the model.}

\section{Language Executors}
\label{app:executors}
\label{app:executors}

Each language requires its own execution contract, and three of the nine broke a structural
assumption that the previous ones had shared. We document them because anyone extending
\textsc{Conseq} will meet the same wall: \textbf{MultiPL-E's \texttt{tests} field is not uniformly
self-executing}.

\begin{table}[H]
\centering\small
\begin{tabular}{lllp{5.2cm}}
\toprule
Lang & Parse check & Execute & Reference-error class (artifact) \\
\midrule
JavaScript & \texttt{node -{}-check} & \texttt{node} & \texttt{ReferenceError}, \texttt{TypeError} \\
TypeScript & \texttt{tsc} TS1xxx & \texttt{tsc} $\rightarrow$ \texttt{node} & \texttt{Cannot find name} \\
Python & \texttt{compile()} & \texttt{python3} & \texttt{NameError}, \texttt{AttributeError} \\
Ruby & \texttt{ruby -c} & \texttt{ruby} & \texttt{NameError}, \texttt{NoMethodError} \\
PHP & \texttt{php -l} & \texttt{php} & \texttt{Error}, \texttt{ArgumentCountError} \\
Perl & \texttt{perl -c} & \texttt{perl} & \texttt{Undefined subroutine}, \texttt{Global symbol} \\
Go & \texttt{gofmt -e} & \texttt{go test} & \texttt{undefined:}, \texttt{declared and not used} \\
Java & \texttt{javac} & \texttt{java -ea} & unresolved symbol \\
Rust & bare \texttt{error:} & \texttt{rustc} $\rightarrow$ binary & \texttt{E0425}, \texttt{E0433}, \texttt{E0412} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Separating syntax from types.} Four languages permit a parse check independent of type
checking, which is what makes $K^{\mathrm{type}}$ measurable. \texttt{gofmt -e} parses without
type-checking; \texttt{tsc} error codes separate TS1xxx (syntax) from TS2xxx (type); \texttt{rustc}
emits bare \texttt{error:} for syntax and \texttt{error[E....]} for type and borrow failures. The
remaining five languages have no such phase and $K^{\mathrm{type}}$ is always false for them.

\paragraph{Harness idiosyncrasies.} Go tasks are test files requiring \texttt{go test}. Perl tasks
define \texttt{testhumaneval} without invoking it and depend on \texttt{Test::Deep}. TypeScript tasks
declare \texttt{require} themselves because \texttt{tsc} has no Node type declarations. Java requires
\texttt{-ea} or assertions are silently skipped and every program passes.

\paragraph{Environment failures are not code properties.} A \texttt{rustc} linker failure carries no
error code and would otherwise be classified as a syntax error, making a language appear entirely
unparseable on a machine without a working linker. Such failures are classified as unmeasured.

\section{Criterion Definitions}
\label{app:criteria}

All eleven criteria are functions of distributions already computed during trajectory generation,
so the marginal cost of the audit is two additional forward passes per trajectory, one for the
reference model behind excess loss and one for the teacher behind divergence, with no extra
generation.

Each criterion is scored in the direction \emph{its own literature} uses it: entropy, negative
log-likelihood, excess loss, and divergence select high values; margin selects low values, since low
margin denotes uncertainty; prefix methods select early positions. Scoring a criterion in the
opposite direction to its intended use would be a straw man, and Table~\ref{tab:criteria} reports
separately whether a criterion would perform better reversed. Eight of eleven would.

Because an error in one of these definitions produced correction B11, we state all eleven as
formulas instead of describing them. $p$ is the student's next-token distribution at the position,
$y_t$ the emitted token, $H$ its entropy, $L$ the trajectory length.

\begin{table}[H]
\centering\small
\begin{tabular}{llll}
\toprule
\# & Criterion & Score at position $t$ & Selects \\
\midrule
P0 & Random & $u \sim \mathrm{Unif}(0,1)$ & --- \\
P1 & Entropy & $H = -\sum_v p_v \log p_v$ & high \\
P2 & Margin & $p_{(1)} - p_{(2)}$ & low \\
P3 & Negative log-likelihood & $-\log p_{y_t}$ & high \\
P4 & Excess loss & $L_\theta(y_t) - L_{\mathrm{RM}}(y_t)$ & high \\
P5 & Teacher--student KL & $\sum_v p^{\mathrm{T}}_v (\log p^{\mathrm{T}}_v - \log p_v)$ & high \\
P6 & Expert--generalist delta & $\frac{1}{|V|}\sum_v \left| \log p^{\mathrm{T}}_v - \log p^{\mathrm{G}}_v \right|$ & high \\
P7 & Position & $t / L$ & low (early) \\
P8 & Confidently-wrong & $\mathbf{1}[H < 0.5 \;\wedge\; \arg\max_v p_v \neq y_t]$ & high \\
P9 & Varentropy & $\sum_v p_v (-\log p_v - H)^2$ & high \\
P10 & Min-$p$ width & $\left| \{ v : p_v \geq p_{\mathrm{base}}\, p_{\max} \} \right|$ & high \\
\bottomrule
\end{tabular}
\caption{The eleven criteria as implemented. P4 follows \citet{lin2024rho} eq.\ 3; the operand order
is the subject of correction B11. P10 follows \citet{nguyen2024minp} eq.\ 1 with
$p_{\mathrm{base}} = 0.1$, within the range that paper uses (0.05--0.3) but a single point in it.
P5 uses the forward KL from teacher to student, the direction used in knowledge distillation. P6 is
our operationalisation of the delta-teacher family as a mean absolute log-ratio over the vocabulary;
the underlying literature applies the log-ratio to a candidate token during decoding rather than
aggregating it into a per-position score, so this row is a reasonable reading and not a
transcription. P8 follows \citet{xu2026tip}, whose criterion is low student entropy together with high
teacher--student divergence, meaning the student is confidently wrong. We approximate that term by
whether the student's argmax differs from the emitted token, and the entropy threshold of 0.5 nats
is a parameter we chose. Rows marked $^\ast$ in Table~\ref{tab:criteria} are the ones whose
per-position form is ours, not a single paper's.}
\label{tab:critdefs}
\end{table}

\paragraph{Roles of the four models.} The student is the model whose trajectory is measured
(Qwen2.5-Coder-1.5B); the reference model for P4 is Qwen2.5-0.5B; the teacher for P5 and P6 is
Qwen2.5-Coder-3B; the generalist for P6 is Qwen2.5-1.5B. All four share a tokenizer, without which
the vocabulary-aligned criteria would not be computable.

Consequence Capture at budget $b$ is
\[
\mathrm{CC}@b = \frac{\left|\{\, t \in \text{top-}b\% \text{ by criterion} \;:\; K^{\mathrm{sem}}_t = 1 \,\}\right|}
{\left|\{\, t \;:\; K^{\mathrm{sem}}_t = 1 \,\}\right|},
\]
so a random selector gives $\mathrm{CC}@b \approx b$ and the ground-truth selector gives the maximum
attainable. This is the operative metric: it states directly how much of the available signal a
criterion buys at the budget practitioners actually use.

\section{Pre-Registration and Outcomes}
\label{app:prereg}
\label{app:prereg}

Gates and kill criteria were fixed in a design document before measurement began. We report each
with its outcome, including the two that failed, because a dataset whose analysis decisions were
made after seeing the data is worth less than one whose were not.

\begin{table}[H]
\centering\small
\begin{tabular}{llp{6.4cm}}
\toprule
Gate & Outcome & Note \\
\midrule
G1 instrument validity & passed & after eleven corrections (Appendix~\ref{app:corrections}) \\
G2 signal non-degenerate & passed & consequential fraction 10--30\% across languages \\
G3 orthogonality & passed & pre-registered as unfalsifiable: either sign was publishable \\
G4 repairability structured & passed & bimodal, replicated across languages and scale \\
G5 consequence predictable & passed & syntactic role, no learned parameters \\
G6 decoding gain & \textbf{failed} & reported in full; the family is closed \\
\bottomrule
\end{tabular}
\end{table}

Two pre-registered predictions were wrong and are reported as such. The consequential fraction was
predicted to \emph{fall} with model scale; it is flat at 15--16\% across a sixfold parameter range.
And an early hypothesis that consequence would not compose was retracted after the definitional
error recorded in B3.

G6 deserves emphasis. The pre-registered response to its failure was to report it as a negative
result instead of searching for a configuration in which it succeeded. We followed that, and the
oracle experiment of \S6.7 is the result.


\section{Exact Values for the Figures}
\label{app:figvalues}

Figures~\ref{fig:typing} and~\ref{fig:boundary} are drawn from the tables below. One script
produces the figure and the table from the same computation, so the two cannot disagree.

\begin{table}[H]
\centering
\begin{tabular}{llrrr}
\toprule
Language & Discipline & $K^{\mathrm{syn}}$ & $K^{\mathrm{type}}$ & $K^{\mathrm{sem}}$ \\
\midrule
Ruby       & dynamic & 31.8\% & --- & \textbf{37.3\%} \\
Perl       & dynamic & 23.8\% & --- & \textbf{32.7\%} \\
PHP        & dynamic & 29.7\% & --- & \textbf{29.2\%} \\
Python     & dynamic & 41.8\% & --- & \textbf{28.4\%} \\
JavaScript & dynamic & 29.7\% & --- & \textbf{24.7\%} \\
\midrule
Go         & static (structural) & 30.0\% & 14.6\% & \textbf{13.2\%} \\
Java       & static (nominal)    & 29.2\% & 19.7\% & \textbf{11.7\%} \\
Rust       & static (affine)     & 29.0\% & 24.3\% & \textbf{11.9\%} \\
TypeScript & gradual             & 31.6\% & 18.3\% & \textbf{9.6\%} \\
\bottomrule
\end{tabular}
\caption{Semantic consequence by typing discipline (Qwen2.5-Coder-1.5B). The dynamic and typed
ranges do not overlap. $K^{\mathrm{type}}$ is defined only where a type-check phase is separable
from parsing. StarCoder2-3B reproduces the strictness gradient: Rust 29.1\%, Java 21.8\%,
TypeScript 18.8\%, Go 12.8\% (same ordering).}
\label{tab:typing}
\end{table}

\begin{table}[H]
\centering\small
\begin{tabular}{lrrrrrrrr}
\toprule
Simultaneous edits $m$ & 1 & 2 & 3 & 5 & 8 & 12 & 20 & 32 \\
\midrule
Pass rate & 100.0\% & 100.0\% & 100.0\% & 99.5\% & 100.0\% & 99.4\% & 94.6\% & 92.3\% \\
\bottomrule
\end{tabular}
\caption{Dose-response for composition. Program pass rate after substituting the second choice at $m$ individually harmless
positions simultaneously. $m=1$ must be 100\% by construction and is the sanity check that the free
set is correctly defined; an earlier version read 66.8\% and was diagnostic of a definitional error
(Appendix~\ref{app:corrections}, B3).}
\label{tab:compose}
\end{table}

\section{Datasheet for \textsc{Conseq}}
\label{app:datasheet}

Following \citet{gebru2021datasheets}. Answers are specific to the release described in this paper
(instrument version 2, 143,786 positions).

\subsection*{Motivation}

\paragraph{For what purpose was the dataset created?} To test, rather than assume, that a model's
uncertainty at a token tracks whether that token's value changes the outcome. Four literatures route
compute or supervision on that assumption; none had ground truth against which to check it. The gap
is not conceptual but empirical: the counterfactual ``what if the model had committed differently
here'' is unobservable in open-ended text, and observable in code only because a test suite
adjudicates it.

\paragraph{Who created it and who funded it?} Stated in the acknowledgements; DMLR review is
single-blind and the author list is not withheld.

\subsection*{Composition}

\paragraph{What do the instances represent?} One instance is one \emph{token position} in one
verified-correct greedy trajectory produced by one model on one benchmark task, together with the
execution verdict obtained when the model's own second choice is substituted at that position. An
instance is therefore a measured counterfactual, not a piece of source text.

\paragraph{How many instances are there?} 143,786, spanning 14 models, 9 model families, 9
programming languages, 3 benchmarks, and 28 model-language configurations. Per-language and per-run
breakdowns are in Table~\ref{tab:coverage} and Appendix~\ref{app:manifest}.

\paragraph{Is it a sample or the complete set?} Complete, conditional on retention. Every position of
every retained trajectory is measured; there is no position-level sampling. Retention is not
random, since a trajectory enters only if it passes its tests, so the dataset is systematically better
populated for more capable models. This is a property of the estimand (attribution inside an
already-broken program is meaningless), not a sampling decision, and it is the dominant selection
effect in the data.

\paragraph{What data does each instance consist of?} The columns of
Table~\ref{tab:schema}: task and position identifiers, the emitted token and its alternative, the
four verdict flags, the model's entropy and margin at that position, provenance (model, family,
parameter count, language, typing discipline), and the instrument version.

\paragraph{Is there a label?} Yes: the four-way verdict, of which $K^{\mathrm{sem}}$ is the primary
target. It is a deterministic function of the substitution, not an annotation.

\paragraph{Is any information missing?} $K^{\mathrm{type}}$ is undefined for the five languages whose
toolchains do not separate type checking from parsing, and is stored as false there instead of as a
distinct missing value; consumers must condition on language. Timed-out executions
($K^{\mathrm{timeout}}$, 0.3--1.4\% by language) are genuinely missing measurements and are excluded
from consequence by construction, not counted as negative.

\paragraph{Are there errors, noise, or redundancies?} Three classes of instrument artifact are
identified and flagged instead of silently removed (\S\ref{sec:artifacts}); they account for
34.0\% of raw $K^{\mathrm{sem}}$ positions and their rate is tokenizer-specific
(19.9--46.2\% across the six adequately-powered families). Ten measurement errors found during development are documented in
Appendix~\ref{app:corrections}, including the two whose published claims were retracted.

\paragraph{Does it contain confidential or offensive content, or identify people?} No. Instances
derive from HumanEval and MBPP, both public benchmark suites of small self-contained programming
tasks. There is no user data, no personally identifiable information, and no free text beyond
programming-language tokens.

\subsection*{Collection}

\paragraph{How was the data acquired?} By direct measurement, with nothing observed or inferred.
For each task the model generates greedily; trajectories that fail their tests are discarded. For
each position of a retained trajectory, the model's second-choice token is spliced in, the suffix is
held fixed, and the program is executed once against its own test suite.

\paragraph{What mechanisms were used?} Open-weight models under \texttt{transformers} at
\texttt{float16} on a single T4 GPU; nine language toolchains for execution
(Appendix~\ref{app:executors}). No human annotation is involved at any stage.

\paragraph{Over what timeframe?} Measurements were collected over a single development cycle; every
row carries an instrument version so that rows collected under different semantics cannot be pooled.

\paragraph{Were ethical review processes conducted?} Not applicable: no human subjects, no personal
data.

\subsection*{Preprocessing, cleaning, labelling}

\paragraph{Was preprocessing done?} Trajectory retention (pass-or-discard) and artifact
classification. Artifact flags are stored as columns; nothing is deleted, so an analyst who disagrees
with our taxonomy can recompute from the raw verdicts.

\paragraph{Is the raw data available?} Yes. Per-run outputs are released alongside the assembled
file, and \texttt{assemble.py} reconstructs the release from them in one command.

\subsection*{Uses}

\paragraph{What tasks is it suitable for?} Scoring and comparing token-selection criteria against
execution-grounded ground truth; studying how consequence distributes over syntactic role; measuring
how a language's type discipline relocates faults.

\paragraph{What should it \emph{not} be used for?} Two uses are contraindicated by our own
measurements. It should not be used to justify a decode-time position-selection policy: consequence
labels are properties of a fixed trajectory and stop applying once decoding leaves it, and a perfect
oracle is statistically indistinguishable from random position selection
(\S6.7). And $K^{\mathrm{sem}}$ should not be compared across languages of different
typing disciplines without adding $K^{\mathrm{type}}$; the two ranges are disjoint for reasons that
are a property of the toolchain and not of the model.

\paragraph{Is there anything that might cause unfair treatment?} The retention asymmetry means claims
supported by this dataset are better evidenced for capable, code-specialised models than for smaller
or general-purpose ones. Three runs fall below 25 retained problems and are flagged in the manifest
and excluded from headline claims.

\subsection*{Distribution, licensing, and maintenance}
\label{app:licensing}

\paragraph{How is it distributed?} As a single newline-delimited JSON file with its manifest,
assembly pipeline, per-language executors, and this datasheet. The access URL is given in the
Availability section.

\paragraph{Under what license?} The dataset is released under CC BY 4.0 and the accompanying code
under the MIT license. Instances derive from HumanEval (MIT), MBPP (CC BY 4.0), and MultiPL-E
(MIT); all three permit redistribution of derived work with attribution, and all three are cited.
Model weights are not redistributed, only measurements taken with them, and each model is cited to
its own release.

\paragraph{Author statement of responsibility.} The authors bear all responsibility in case of
violation of rights and confirm that the data license permits the release described here.

\paragraph{Who maintains it, and how will errors be handled?} The authors. The corrections log
(Appendix~\ref{app:corrections}) is the maintenance mechanism as much as the record: every entry
names the symptom that exposed the defect, so a consumer who observes the same symptom can identify
the cause. Corrections are released as new instrument versions instead of silent edits, and
\texttt{assemble.py} refuses to merge rows whose column semantics differ across versions. That is the
guarantee that a future correction cannot retroactively contaminate this release.

\paragraph{Will it be updated?} Additional model-language configurations can be added at roughly 0.5
GPU-hours each. Any change to column semantics increments the instrument version and quarantines
incompatible rows instead of pooling them.
